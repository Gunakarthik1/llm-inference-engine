services:
  - type: web
    name: llm-inference-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn engine.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
